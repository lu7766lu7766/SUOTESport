import InterfaceConf from 'src/config/InterfaceConf'
var devTitle = require('src/../env').devTitle

module.exports = {
	branchSource: `ibet66.cc`,
	fileHost: `//ibet88.cc`,
	apiHost: `/api/`,
	paymentHost: `//${devTitle}.${InterfaceConf.Host.Payment}:${location.port}`
}
